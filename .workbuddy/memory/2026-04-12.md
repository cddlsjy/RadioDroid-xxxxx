# 2026-04-12 工作日志

## GitHub Actions CI/CD 工作流搭建

- **修复**: `gradle/wrapper/gradle-wrapper.properties` 的 `distributionUrl` 从本地路径 `file:///D:/gradle/gradle-8.5-bin.zip` 改为官方地址 `https://services.gradle.org/distributions/gradle-8.5-bin.zip`，否则 CI 环境无法构建。

- **升级**: `.github/workflows/android-ci.yml`
  - JDK 11 → 17（与 app/build.gradle 的 Java 17 配置一致）
  - 新增 Gradle 依赖缓存（`actions/cache@v4`）
  - 拆分 lint / unit test / assemble 三个独立 step
  - 上传 lint 报告、测试报告、debug APK 为构建产物

- **新建**: `.github/workflows/release.yml`
  - 触发条件：推送 `v*` 格式 tag（如 `v0.93`）
  - 构建 `freeRelease` 和 `playRelease` 两个 APK
  - 支持通过 Secrets（`KEYSTORE_BASE64` 等）注入签名密钥
  - 自动创建 GitHub Release 并上传 APK（使用 `softprops/action-gh-release@v2`）

- **升级**: `.github/workflows/android-instrumented-tests-ci.yml`
  - 所有 actions 版本升至 v4
  - JDK 11 → 17
  - `fail-fast: false`（矩阵任务互不影响）
  - 增加测试结果产物上传
  - AVD 缓存 key 加版本后缀 `-v2` 强制更新

## 项目关键信息
- 应用包名：`net.programmierecke.radiodroid2`
- 版本：0.93 (code 102)
- 两个产品风味：`free`（F-Droid，无 GMS）、`play`（Google Play，含 Cast）
- Gradle 8.5 + AGP 8.0.0 + JDK 17
